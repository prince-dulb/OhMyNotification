# 救救我的通知！项目规则

## 项目定位

本项目首先是一个个人 Android 通知收件箱：默认检测所有应用的通知，可靠保存用户未排除的通知，并尽可能保留原通知的可执行跳转。

代码和产品边界必须为未来其他后台需求留出扩展空间，但具体是否采用“宿主 + 模块”架构仍需在落地阶段确认。扩展空间不等于一个永不退出的通用进程；每类任务必须按 Android 平台模型选择 `NotificationListenerService`、`WorkManager` 或前台服务。

## 当前阶段

- 2026-09-05：用户已确认当前日用版本可作为 `v1.0.0` 发布，并授权本次 GitHub push 与公开 Release。功能范围冻结，不再为 v1 增项。源码为 `1.0.0` / `versionCode=12`，独立正式签名和 Debug→Release 证书轮换谱系已生成；旧 Debug 证书只保留既有数据迁移能力，回滚能力关闭。红魔 Android 16 已从 `0.1.9-mvp` 成功覆盖升级到正式版，首次安装时间、通知权限、监听授权和 530 条记录保持，进程与常驻状态通知正常。GitHub CLI 认证有效，当前只剩公开候选树检查、提交、远端仓库、标签与 Release。
- 2026-08-29：`0.1.9-mvp`（`versionCode=11`）新增任意通知记录的显式逐条删除：每条内容底部提供“删除记录”，确认框固定显示“删除 1 条记录”，说明不可恢复、不改变来源监控，且来源再次发送或更新时记录可能重新出现。成功删除只移除该通知事实和当前进程动作句柄；健康证据、蓝黄时间轴、排除策略和查看筛选保持不变，常驻状态摘要会重算数量与最新记录。Room schema 保持 v1。21 个 JVM 测试套件共 92 项、129 任务无设备总门、APK 边界、可安装性、测试数据和敏感扫描通过；删除隔离的 Room instrumentation 测试已编译但未在真实日用数据进程上运行。本地侧载 APK SHA-256 为 `85109e10e959b9209aa6202f35664018ce534c26d223b29d84b43efbceef3acd`。已覆盖安装红魔真机并保留 387 个通知条目、350 个移除事实和 22 个监控排除来源；监听与常驻状态通知正常。真机确认首屏有 2 个可见逐条删除入口，确认框打开、取消和进程存活正常；为避免破坏真实历史，本轮没有代替用户确认删除。
- 2026-08-29：`0.1.8-mvp`（`versionCode=10`）把“无法跳转归档”校正为按当前能力动态派生的“当前无跳转动作归档”。归档条目现在可以由用户点按触发一次有界的系统活动通知重扫；重扫继续复用既有身份与生命周期归并，只有重新关联到同一 `itemId` 的动作才会立即尝试派发，失败不重试、不打开来源首页。该路径没有新增轮询、前台服务或周期任务，Room schema、通知事实、排序与采集策略未变。21 个 JVM 测试套件共 92 项、129 任务无设备总门、APK 边界、可安装性、测试数据和敏感扫描通过；本地侧载 APK SHA-256 为 `d3dd1f72014a4a225fb132646b75b225ad34fd19acee756ea611ec648fdacedf`。已覆盖安装红魔真机并保留 387 个通知条目、350 个移除事实和 22 个监控排除来源；监听与常驻状态通知正常，当前系统活动通知恢复出 27 个运行时动作，重复身份/代次组为 0。安装时设备处于锁屏通知层，本轮未冒充完成归档点击的真机 UI/落页验收。
- 2026-08-29：`0.1.7-mvp`（`versionCode=9`）把来源级“更多”菜单从每条通知提升到应用抽屉标题行，并放在展开/收起控件旁；单条抽屉也使用同一来源标题，通知内容区不再重复 App 管理入口。菜单保留“不要再记录/重新记录这个 App”，新增“仅查看此 App”和“不查看此 App”。查看排除使用独立私有偏好黑名单，并在 SQLite/Paging 查询层按 `AppUserKey` 排除；它不改变监控策略，在“查看全部”下也不会把当前其他 App 固化成白名单，因此未来新来源仍可见。Room schema、通知事实、动作、排序和采集热路径未变。21 个 JVM 测试套件共 91 项、129 任务无设备总门、APK 边界、可安装性、测试数据和敏感扫描通过；本地侧载 APK SHA-256 为 `f3aabc96bfb90cb43cbedb91c78e8bcf139bdcc65be5ef3740183e02d94239d9`。已覆盖安装红魔真机并保留 360 个通知条目、350 个移除事实和 22 个监控排除来源；监听、常驻状态通知正常，归档真机 UI 已确认每条通知级菜单为 0、可见抽屉来源菜单为 1，且菜单同时出现三个预期动作。
- 2026-08-27：`0.1.6-mvp`（`versionCode=8`）把默认收件箱收窄为当前仍持有原通知动作的条目，动作提示改为“点按尝试打开原内容”；没有动作、进程重建后未恢复动作、动作明确取消或系统明确拒绝的长期记录动态进入设置页可访问的“无法跳转归档”。归档继续使用来源筛选、稳定排序、抽屉与分页；能力集合直接进入 SQLite 查询条件，不做加载后隐藏，补扫重新取得动作时条目自动回到默认收件箱。逐条失效提示和“仅打开来源应用”降级已移除；既有 B 站样本没有持久 URI/内容 ID，未伪造链接。Room schema、通知事实和历史记录未变。21 个 JVM 测试套件共 89 项、129 任务无设备总门、APK 边界、可安装性、测试数据和敏感扫描通过；本地侧载 APK SHA-256 为 `c8b32135ed4d38a9eb96be15bdb4e9fba751aa1b148c36319ed3edd74feb2faf`。已覆盖安装红魔真机并保留 305 个通知条目、270 个移除事实和 16 个排除来源；当前恢复 26 个运行时动作，设置页显示 279 条无法跳转归档，默认收件箱新文案、归档入口/页面和旧失效控件消失均完成真机 UI 检查，监听与常驻状态通知正常。
- 2026-08-27：`0.1.5-mvp`（`versionCode=7`）新增统一设置页：媒体播放、消息与通话、闹钟/提醒/导航、持续运行/进度、其他通知五类可独立开关，媒体默认关闭且过滤发生在正文与动作复制前；跨应用抽屉时间窗提供 5、15、30 分钟与 1、2、6、12、24 小时选项，连续同源仍不受时间跨度限制；收件箱顶部“不监控”入口移入设置页，每条通知的快速排除入口保留。配置使用私有偏好和原子内存策略快照，Room schema、身份和排序键未变。21 个 JVM 测试套件共 88 项、129 任务无设备总门、APK 边界、可安装性、测试数据和敏感扫描通过；本地侧载 APK SHA-256 为 `472f67483ee3528a3f6e3dbe98828d01bf0af7b0ee5c9d01f69c61ea323b940d`。已覆盖安装红魔真机并保留 305 个通知条目、270 个移除事实和 16 个排除来源；监听与常驻状态通知正常，设置页五类开关、两列八档时间窗和排除管理弹窗均完成真机可见性/打开检查，默认时间窗恢复为 15 分钟。真实媒体通知的长期过滤效果留给日用通知验证。
- 2026-08-25：`0.1.4-mvp`（`versionCode=6`）按日用反馈把抽屉策略升级为 `ANCHOR_V2`：当前可见流中连续同源通知即使相隔很久也合并；跨过其他 App 后仍受 15 分钟与最多 3 条限制；查看筛选先于分组。抽屉默认展开，仍可手动收起。为避免单 App 长抽屉在首屏自动预取至 Paging 的 200 条上限，加载触发改为用户真正滚到已加载内容底部附近才取下一页。20 个测试套件共 83 项、129 任务完整构建门、APK 边界、可安装性、测试数据和敏感扫描通过。本地侧载 APK SHA-256 为 `7a84d2822ee75c8ad9147ccb1191d5c34aba11a563c30fe6b35aab6424d0b5ad`；已覆盖安装到红魔真机并保留 175 个通知条目、135 个移除事实和 13 个排除来源，监听、常驻状态通知正常，重复身份/代次组与当前进程错误日志均为 0。自动 UI 树未在首屏捕获到抽屉头，因此默认展开的真机肉眼验收仍由用户日用确认；Room schema 保持 v1。
- 2026-08-24：一次对抗式可见缺陷审查确认并修复了四类问题：Debug 专用统计/解绑入口可被普通第三方应用显式调用；监听串行消费者会被单条异常永久杀死且回调队列满后会在主线程 `runBlocking`；监听仍连接时处理链故障会被错误显示为健康；来源应用跳转的 `SecurityException` 与 Paging 加载失败没有安全反馈。Debug 外部控制入口现受系统 `DUMP` 权限保护，ADB shell 工具仍可调用；监听工作改为单条异常隔离、非阻塞入队和显式处理健康状态，后续成功事件可恢复；列表首屏/翻页失败提供重试。Room schema 保持 v1，补扫仍按首次观察时间排序，缺失移除回调下的 key 复用歧义未用猜测改写。19 个测试套件共 77 项、129 任务完整构建门、APK 边界、可安装性、测试数据和敏感扫描通过。当前 Debug 已覆盖安装到红魔真机且保留既有数据：监听连接、常驻状态通知、通知权限与渠道均正常，85 个通知条目、67 个移除事实和 6 个排除来源仍在，重复身份/代次组为 0，当前进程错误日志为 0；这证明安装与即时运行正常，长待机和漏记率仍需真实通知观察。
- 2026-08-24：`0.1.3-mvp`（`versionCode=5`）把监控排除入口移到每条既有通知的 Material 3“更多”菜单；用户可直接选择“不再记录这个 App”，确认文案明确只影响未来通知、历史保留且 schema v1 对同包所有资料生效。已排除来源可从同一菜单“重新记录这个 App”，排除列表继续作为完整管理入口。红魔真机用两个合成来源完成排除、确认框即时关闭、排除后新通知不入库、历史保留、恢复监控和测试策略回滚；测试前后的真实排除数量保持 2，Room schema 仍为 v1。129 任务构建门、APK 边界、签名/对齐/Android 16/64 位、敏感扫描和覆盖安装冒烟通过。
- 2026-08-23：用户把首版交付口径明确为“完成最基础、最核心的功能即可，有 bug 或新需求边用边提”。因此，能在目标手机上完成全量采集、历史查看、系统通知划除后回开、来源筛选/排除、常驻状态和基本健康提示，即可作为首个可日用 MVP 发机；投稿/动态/开播逐类验收、重启与长待机矩阵、工作资料、release 签名和完整性能预算改为后续迭代证据，不再阻挡首个 MVP 侧载，但所有已知边界必须诚实保留。
- 2026-08-23：首个侧载版本定为 `0.1.0-mvp`（`versionCode=2`），完整无设备构建门、APK 边界、签名/对齐/Android 16/64 位和敏感扫描通过，并已覆盖安装到红魔 11 Pro+。安装后 49 个逻辑条目与 39 个移除事实仍在，重复身份/生命周期组为 0；监听经一次有界自动重绑恢复，常驻状态通知已重新出现且公开锁屏版本不含最近标题或正文。
- 2026-08-23：用户发现 `0.1.0-mvp` 点击“查看”和“不监控”均崩溃；红魔 DropBox 证明两者共用的 Compose `LazyColumn` 把不可写入 Android `Bundle` 的 `AppUserKey` 用作 item key。`0.1.1-mvp`（`versionCode=3`）改用现有无歧义编码生成字符串 key，并增加回归测试。完整构建与 APK 门通过后已覆盖安装；真机分别打开并关闭两个弹窗，进程保持存活且 DropBox 无新增 crash，监听、状态通知和 53 个逻辑条目正常。
- 2026-08-23：用户报告“请求重新连接监听”无效。受控断连证明 Android `requestRebind()` 与页面点击链路本身可用，根因是自动请求和用户请求共用 10 秒冷却，刚发生的自动请求会拦掉用户点击，且结果仅以短暂 Snackbar 呈现。`0.1.2-mvp`（`versionCode=4`）将用户请求改为独立 1 秒防连点、允许绕过自动冷却与每进程自动次数上限，并在断连界面保留结果及通知使用权设置入口。129 任务完整构建门、APK 边界、敏感扫描和安装冒烟通过；红魔受控断连后从页面点击记录为 `USER / REQUESTED` 并在 1 秒内恢复，56 个逻辑条目与 44 个移除事实保留，重复身份/生命周期组为 0，常驻状态通知正常。
- 2026-08-23：生产 MVP 日用 Debug 已实现并覆盖安装到目标红魔设备。Room schema 保持经批准的 v1；当前保存 49 个逻辑条目，其中 39 个已收到移除回调，重复身份/生命周期组为 0。用户已确认一条类型尚未分类的真实 B 站通知在原通知划除后可从 OMN 归档精确落页；该结果发生在同一 OMN 进程生命周期内，覆盖安装也验证了被划除通知的运行时动作不会跨进程恢复，最近一次正常连接从 6—7 条活动通知恢复动作句柄。重连恢复按 `itemId` 注册当前进程动作，内容未变化的活动通知不为刷新进程会话而改写 Room。常驻状态卡已补齐进程重建后的最近来源/标题恢复，区分未授权、等待连接、已连接和曾连接后中断，并按语义模型抑制重复 `notify()`；目标设备已授予 `POST_NOTIFICATIONS`，固定渠道启用。真机曾复现“状态卡已不存在但相同语义签名阻止补发”，现改为只有签名相同且通知仍活跃时才跳过发布；Debug 定向取消后为不活跃，Activity 重新进入前台后已自动补回。“只查看”筛选使用独立私有偏好、草稿—应用/取消交互和 `sourceUserRef + sourcePackage` 复合键；筛选目录只列已有历史与已选项，工作资料真机 UI 仍未验证。监控排除以 Room 为唯一持久真相并在 `Application.onCreate()` 返回前恢复热路径快照；受 schema v1 限制，它仍按包名作用于所有资料，升级为复合键必须重新经过数据库审批。覆盖安装曾两次出现授权仍在但 25/60 秒不重连；生产候选现提供 3 秒延迟、10 秒冷却、每进程最多 3 次的有界 `requestRebind()` 与用户手动入口，受控解绑实测第 3 秒自动恢复且句柄未丢。P0-109 已把同生命周期正文更新造成的健康事实写放大限制为当前连接每 5 分钟至多一条，同时保持通知正文即时提交；动态更新真机 A/B 仍待下一次样本。加入自愈后的真正后台冻结 30 秒为 CPU 增量 0 ticks、数据库增长 0 B，证明没有新增轮询，但仍只是 Debug 短样本。正文更新合并因存在末次状态新鲜度取舍而尚未启用。B 站投稿/动态/开播三类分别确认、筛选/排除的用户验收、锁屏脱敏/重启/长待机与 release 性能基线继续作为后续迭代项，不冒充已经验证。
- `plan-v1.0` 已于 2026-08-22 经用户确认，G0 通过，项目正式进入 Phase 0。
- P0-001—P0-005 已完成。2026-08-23，P0-004 的独立受控通知源已在红魔 11 Pro+ / Android 16 上通过发布、同身份更新、真实落地回执和移除合同；P0-101 已取得监听器私有事件流，并在不强停 OMN 的直驱链路中捕获同一系统身份的发布、更新、移除三回调。B 站私有流现有 91 条回调、31 个唯一系统身份，其中 90 条是三次重连对 30 个活动通知的重复补扫，1 条是连接期间新推送；可见文本只形成视频/动态候选，尚未由用户确认三类。P0-102 已证明原系统通知移除后，当前 OMN 进程内的受控 `PendingIntent` 仍可派发并真实落页；Android 16 要求发送方从可见 Activity 使用 `MODE_BACKGROUND_ACTIVITY_START_ALLOW_IF_VISIBLE`，裸 `send()` 只返回接受但不会落页。P0-103 在当前 31 个 B 站样本中未观察到 URI、BV/AV 号或其他持久精确目标，31/31 只有运行时 Activity 动作。P0-104 与 P0-108 的摄取/归并及排序/筛选/抽屉纯 Kotlin 逻辑基线已通过，P0-107 的保守健康区间逻辑候选也已通过，但其红魔盲对账仍依赖 P0-105。P0-106 已完成 Android 16 平台合规基线，普通 ongoing 为默认候选，`specialUse` FGS 只保留为待真机 A/B 的非默认候选。Room/release 真机性能仍归 P0-109。B 站三类用户确认、动作长期边界、锁屏/重启可靠性和 FGS A/B 仍待验证。独立的 Phase 0 应用 APK 也已完成安装、启动、进程和 resumed activity 冒烟验收；该结果只证明工程壳可运行。
- 当前按 `docs/开发计划书/任务拆分.md` 执行：先本地合并并标记已确认计划，再只读盘点现有工具；盘点完成后必须先更新本文件的目录与验证规则，才可创建 Android 验证工程。
- Phase 0 只验证致命技术假设和资源边界，不把 Spike 代码冒充产品功能，也不提前创建未经 G3 批准的生产 Room schema。
- GitHub 远端、push、CI、公开 release、全局安装和系统配置仍分别受 G1/G5 与用户红线约束；G0 不构成这些操作的授权。
- 已确认事实、候选方案和待验证假设必须分开书写，不得把推测升级为需求或技术结论。

## 文档权威顺序

发生冲突时按以下顺序执行：

1. 用户在当前会话中的明确指令
2. 本文件 `AGENTS.md`
3. `docs/项目计划书.md`
4. 后续详细开发计划与功能规格
5. 代码注释和历史记录

如果实践需要改变规则，先修改对应文档并获得必要确认，再修改实现。

## 目录约定

- `docs/`：项目定义、架构决策、功能规格、测试记录和用户文档。
- `docs/开发计划书/`：详细开发计划总索引、路线图、架构、非功能需求、测试策略、任务拆分和计划变更记录。
- `docs/开发计划书/specs/`：每个 `[A]`、`[B]` 功能各自的独立规格文件；文件名使用稳定的中文功能名，不使用版本号或完成状态后缀。
- `docs/phase-0/`：可公开的 Phase 0 工具链基线、Spike 协议、去敏结果和决策矩阵；文件名以对应任务或 Spike ID 开头，例如 `P0-001-toolchain-baseline.md`。真实通知正文、系统动作令牌、本机绝对路径和未脱敏截图不得进入此目录。
- `app/`：唯一 Android 应用模块。`src/main/` 只放可进入 release 的壳与实现，`src/debug/` 放不会进入 release 的 Phase 0 调试入口，`src/test/` 放 JVM 测试，`src/androidTest/` 生成独立测试 APK并承载可控通知源；不得另建平行的产品应用绕过正式构建。测试 APK 使用 `.test` 应用 ID 后缀，只能声明测试所需权限，不得拥有网络、账号或真实用户数据；其组件和标记必须通过 APK 扫描证明不在 production APK 中。
- `app/src/main/java/.../core/`：不依赖 Android framework、Room 或 Compose 的领域类型与纯规则；`model/` 放不可变契约，`normalize/` 放归一化实现。Android `Notification`、`PendingIntent` 和 `StatusBarNotification` 不得进入此目录。
- `app/src/main/java/.../capture/`：Android 通知监听、framework 快照复制、当前进程动作仓储和摄取协调；只通过纯数据投影进入领域/持久化边界，禁止在回调中直接操作 Compose。
- `app/src/main/java/.../data/`：放经 G3 明确批准的 Room entity、DAO、数据库和 repository 实现；用户已于 2026-08-23 批准首次 schema v1，后续任何表、列、索引、版本或迁移变化仍需重新获得数据库红线授权。
- `app/schemas/`：Room 构建导出的版本化 schema JSON，必须跟踪并用于迁移测试；只能由构建生成，不手工改写。
- `app/src/main/java/.../ui/`：Material 3 界面、展示模型与交互协调；不得持有 `StatusBarNotification`、`Notification` 或长期 `PendingIntent` 引用。
- `gradle/`：Gradle Wrapper 与版本目录。Wrapper 脚本、JAR、校验后的固定分发 URL和 `libs.versions.toml` 可以跟踪；下载的分发包与用户级缓存不得复制入库。
- `tools/`：项目内可复现的检查、去敏、夹具和测量脚本；脚本必须有单一入口、非零失败码和简短用法，不得要求全局安装才能运行。
- `testdata/`：T1—T4 共用的版本化去隐私合成数据。每个数据集使用稳定 ID 目录，至少包含数据、版本、生成规则、固定种子、SHA-256、期望摘要和变更原因；输入或期望变化必须新建版本，禁止覆盖旧基线。该目录不得出现真实通知、真实包名、内容 ID、URI、账号或本机路径。
- `.local-evidence/`：仅本机保存的原始 Phase 0 证据、设备输出和真实通知样本，必须被 Git 忽略；需要提交的结论先去敏，再人工写入 `docs/phase-0/`。该目录不自动清理，删除前仍需用户授权。
- `artifacts/`：明确要求保留的本地交付物，不得混放源码。APK 使用 `OhMyNotification-<version>-<variant>.apk` 命名，同时生成同名 `.sha256`；二者默认不进入 Git，公开 release 仍需用户单独授权。
- `.tmp/`：可再生成的临时文件；任务结束前清理，不提交版本库。
- 项目根目录只放全项目级配置、入口文档和构建文件，不堆放调研附件或临时输出。

新增目录前，先在本节补充用途、命名和清理规则。

### 本地配置与生成物

- `local.properties` 只保存本机 SDK 定位，必须被 Git 忽略；任何文档、提交、日志和诊断导出都不得复制其中的绝对路径。
- `.gradle/`、`.kotlin/`、`.idea/`、所有 `build/`、设备捕获和性能原始输出均为本地生成物，不得提交。
- 构建版本使用固定值，不使用 `+`、`latest`、浮动范围或未经校验的预览版。
- Phase 0 默认使用项目 Wrapper 和进程级环境变量，不修改用户或系统级 `PATH`、`JAVA_HOME`、`ANDROID_HOME`。
- 正式工作区必须保持 ASCII 路径。P0-003 曾在非 ASCII 路径完成编译、Lint、Debug 与 Release，但 P0-004 的 Gradle Test Executor 实测会破坏测试类路径并报 `ClassNotFoundException`；用户随后将工作区改为 ASCII 名称，标准 JVM 测试恢复通过。不得重新启用 `android.overridePathCheck` 或用目录联接掩盖该问题。
- v0 保持单 `:app` 模块；出现第二个真实复用边界前，不新增通用宿主、基础设施模块或独立后台进程模块。

## 命名与写作

- 用户文档默认中文；代码、命令、包名、类名和变量名使用英文。
- 应用中文显示名为“救救我的通知！”，英文显示名为“Oh My Notification!”，GitHub 仓库名为 `OhMyNotification`，项目短称为 `OMN`，完整 `applicationId` 为 `io.github.prince_dulb.ohmynotification`。
- 文档使用有意义的稳定名称，不用 `final`、`new`、`最新版` 等状态词。
- 需求和决策统一标记为：`[已确认]`、`[候选]`、`[待验证]`、`[排除]`。
- 时间使用带时区的 ISO 8601；数据库时间统一存 UTC，界面按设备时区显示。

## Android 平台与界面标准

- 以当前 Android 官方开发指南、目标 SDK 要求和 AndroidX 稳定版能力为实现基线；锁定具体版本时记录依据和日期。
- v0 技术栈已确定为原生 Kotlin、Jetpack Compose 和 Room；不采用 Flutter，也不采用 Flutter 与原生混合栈。除非用户明确重开技术栈决策，不得在实现中引入 Flutter 运行时或以 Flutter 插件承载核心能力。
- v0 只面向 Android 16 及以上并采用个人侧载，不为旧系统增加兼容分支；源码公开不等于已经决定进入 Google Play。
- 首要真机验收设备为红魔 11 Pro+（16 GB RAM、1 TB 存储、Android 16、不支持 32 位应用）。目标设备上的结果优先于模拟器推断。
- v0 不得引入仅支持 32 位 ABI 的依赖。若依赖包含原生库，必须在目标设备上确认其 64 位 ABI；能不用原生库时优先保持纯 Kotlin/Java/AndroidX 路径。
- 目标设备资源充裕不构成低效实现的理由；通知列表仍须分页或惰性加载，不得把全部历史长期驻留内存。
- 界面遵循 Material 3。优先使用具备完整状态、语义和无障碍支持的官方组件，不自行仿造已有标准控件。
- 支持 edge-to-edge，并正确处理状态栏、导航栏、显示切口和输入法 inset。
- 布局至少适配手机横竖屏、分屏和不同字体缩放；更大窗口按官方自适应布局原则处理。
- 所有可点击目标至少满足 48dp 触控范围；图标、折叠抽屉和自定义时间轴必须提供可访问语义。
- 健康状态不能只靠颜色表达：蓝色实线表示“已确认运行”，黄色虚线表示“未确认运行”，同时提供文本或无障碍描述。
- 支持浅色、深色和系统动态配色时，健康状态的蓝/黄语义仍需保持，并满足对比度要求。
- 动效只用于解释抽屉展开、列表变化和状态切换；尊重系统“减少动态效果”设置。

## 产品与数据原则

- 本地优先：通知正文和跳转信息默认只保存在设备本地；任何联网能力都必须单独说明目的并由用户决定。
- 本项目不提供已读/未读状态；它负责兜住被划掉的通知，不把归档记录改造成待办收件箱。
- 默认检测所有来源，但媒体播放控件默认不记录；用户可在设置页分别开关媒体播放、消息与通话、闹钟/提醒/导航、持续运行/进度和其他通知类型，并通过排除列表选择不监控的应用。类型或来源策略排除的新通知不得持久化正文或动作；既有历史不受设置变化影响。
- 将应用加入排除列表只影响后续采集，既有记录默认保留；删除记录必须是独立、明确的用户操作。
- 当前 MVP 的逐条删除使用通知内容区内可见的“删除记录”动作和不可撤销确认，不藏在长按/滑动手势里，也不放回逐条“更多”菜单；删除只影响所选条目和内存动作，不改变来源监控，也不删除健康证据、排除策略或查看筛选。来源后续重新发送或更新时允许按正常规则再次形成记录。
- v0 不自动删除长期通知条目，只显示存储占用并限制诊断事件；任何自动清理策略都必须以后单独确认。
- “不监控某应用”和“只查看某些应用”是两套独立状态：前者控制采集与保存，后者只控制当前列表筛选。
- 本应用自己的常驻状态通知必须自动排除，避免自我记录和递归更新。
- 通知记录和通知事件分开建模：同一通知的更新不得无条件插入为新的收件箱条目。
- 去重不得只比较标题或正文，以免错误合并不同视频、动态或直播通知。
- 默认排序必须稳定、可解释；同一排序字段相同时必须有确定的次级顺序。
- 应用内按时间轴纵向展示通知；连续同源通知不受时间跨度限制，跨越其他来源时按设置页保存的时间窗与最多 3 条规则折叠为抽屉。抽屉必须显示覆盖时间范围并保留内部逐条时间；超过分组时间阈值后恢复严格全局时间排序。
- 应用外提供低打扰的常驻状态通知，展示监听状态、累计或近期记录数量及最近记录来源；默认文案使用“记录”而非暗示阻止送达的“拦截”。
- 解锁状态下，常驻状态通知可显示最近来源应用和通知标题；锁屏状态只显示来源应用，不显示原通知正文。
- 常驻通知的存在不能被当作唯一健康证据。通知权限、通知渠道、监听连接和进程证据必须分别建模；无法确认的时间段标记为“未确认运行”。
- “能查看文字”不等于“可用”：可跳回原内容是通知模块的核心验收项。
- 归档记录不设置人为的跳转有效期；只要记录存在，就保留所有受支持的动作引用与持久目标。但 `PendingIntent` 可能被创建方取消、设为一次性或因系统状态失效，也没有受支持的磁盘持久化格式；是否能长期持有动作，以及能否提取 B 站内容 ID、URI 或其他持久备用目标，属于 Phase 0 技术验证。
- 默认收件箱只展示当前进程仍持有原通知动作或未来已取得持久精确目标的条目；按钮使用“尝试打开原内容”，不得把系统接受派发写成已精确落页。没有精确候选、进程重建后动作未恢复、动作明确取消或系统明确拒绝的长期条目进入“当前无跳转动作归档”，从设置页可查看；这是按当前能力动态派生的视图，不删除记录、不修改 Room schema。归档条目可由用户点击触发一次有限的活动通知重扫，只有按既有身份与生命周期重新关联到同一 `itemId` 的动作才可立即尝试派发；失败不自动重试，也不打开来源首页冒充原内容。

## 性能与资源纪律

- 性能占用是硬约束：在不牺牲可靠采集、数据完整和可回开的前提下，选择实测 CPU、内存、唤醒、写盘和耗电最低的方案。
- 无通知且用户未打开界面时应保持事件驱动，CPU 活动和写盘尽可能接近零；禁止轮询通知状态。
- 不得仅为绘制更精细的健康时间轴而持有唤醒锁、使用精确闹钟或高频唤醒。健康证据优先来自监听生命周期、真实通知事件和进程自然活跃时的低频确认。
- 常驻状态通知只在监听状态变化、真实通知到达或必要摘要变化时更新；短时间突发事件必须合并刷新。
- 前台服务会提高进程常驻级别，只能在真机 A/B 证明它对可靠性或常驻状态语义确有必要后采用；服务内部不得运行空转循环。
- 长期数据以压缩后的通知条目为主。原始事件和诊断日志必须最小化、设定容量或时间上限，不得无限增长。
- 默认不为每条通知重复保存应用图标，也不保存非核心的大图或媒体；使用包名引用和受控缓存。
- 数据库写入只由真实事件或明确状态变化触发；同一通知的密集更新应在不增加丢失风险的前提下合并事务。
- 通知列表使用惰性加载或分页、稳定键和增量分组；禁止每次刷新全表载入、全量排序或重算全部抽屉。
- 默认无网络、无遥测、无远程心跳。
- 性能结论必须来自 release 构建和红魔 11 Pro+ 真机测量，不得用 debug 构建体感代替。
- 每个版本记录后台内存、CPU 时间、唤醒、数据库增长、状态通知更新次数和长待机耗电；无解释的明显回退不得合并。

## 工程纪律

- 项目采用 `GPL-3.0-or-later` 许可证；引入依赖、素材或参考实现前必须核对许可证兼容性和署名要求。
- 项目计划在 GitHub 全部开源；所有被 Git 跟踪的文件和提交一律按公开内容处理，真实通知正文、动作令牌、未脱敏截图、签名材料、本机 SDK 路径和个人数据不得进入仓库。
- 不以关闭报错、绕过权限或放宽测试来换取表面可运行。
- 每项实现必须有与风险相称的验证；完成声明必须附命令结果或真机测试记录。
- 自动化测试至少覆盖去重、更新合并、稳定排序、时间窗分组、排除列表、查看筛选和数据持久化。
- 真机测试至少覆盖通知到达、手动划除后查看与跳转、常驻状态通知、通知权限关闭、长时间锁屏、监听重连和手机重启。
- 界面测试至少覆盖字体放大、深浅色、触控范围、无障碍语义、edge-to-edge 和抽屉展开后的时间顺序。
- 性能测试至少覆盖无通知长待机、通知突发、万级历史列表和数据库持续增长；比较监听服务单独运行与附加前台服务两种方案。
- Android 后台可靠性只能通过目标手机实测确认，模拟器或单元测试不能替代。
- 密钥、Token、通知原文和个人数据不得进入代码仓库、提交记录或公开日志。
- 未经用户明确要求，不生成或覆盖发布版 APK，不执行公开发布。

## 当前验证命令

工程创建后，每次相关改动至少运行与范围相称的以下命令；命令尚不存在或失败时不得宣称任务完成：

- Wrapper/环境：`.\gradlew.bat --version`
- JVM 测试：`.\gradlew.bat test`
- 测试数据校验：`.\tools\verify-testdata.ps1`
- instrumentation 测试 APK 编译：`.\gradlew.bat :app:assembleDebugAndroidTest`
- Android 设备测试（有目标设备时）：`.\gradlew.bat connectedDebugAndroidTest`
- Android 安装启动冒烟（有授权设备时）：`.\tools\install-and-smoke-test.ps1`
- Phase 0 私有证据模板（按 Spike）：`.\tools\new-phase0-evidence.ps1 -SpikeId SP-01`
- 打开通知使用权设置（debug 监听器安装后）：`.\tools\open-notification-listener-settings.ps1`
- 请求重绑已获授权的 debug 监听器：`.\tools\request-phase0-listener-rebind.ps1`；此命令不授予权限
- Phase 0 通知事件拉取（debug 监听授权并收到回调后）：`.\tools\pull-phase0-notification-events.ps1`
- Phase 0 受控运行时动作派发：`.\tools\invoke-phase0-controlled-runtime-action.ps1`；只允许测试 APK 创建的动作
- Phase 0 监听状态只读检查点：`.\tools\capture-phase0-listener-state.ps1 -SelfTest`；真机实验时另传 `-Checkpoint` 与 `-RunId`
- 公开候选树敏感扫描：`.\tools\verify-sensitive-content.ps1 -SelfTest`
- Android 静态检查：`.\gradlew.bat lint`
- 调试构建：`.\gradlew.bat :app:assembleDebug`
- 本地未签名/默认签名发布构建检查：`.\gradlew.bat :app:assembleRelease`
- 正式签名与 Debug→Release 证书轮换：`.\tools\sign-release.ps1`；密码只允许在本地安全提示中输入，正式密钥和签名谱系位于仓库外
- production/test APK 边界：`.\tools\verify-apk-boundary.ps1`
- 可安装 Debug APK 校验：`.\tools\verify-installable-apk.ps1`
- MVP 数据库去敏计数：`.\tools\query-mvp-database-stats.ps1`；只允许输出计数、连接布尔值和当前进程动作句柄数，不得扩展为通知正文、来源包名或动作数据导出
- 无设备构建总门：`.\gradlew.bat test lint :app:assembleDebug :app:assembleRelease :app:assembleDebugAndroidTest`

真机命令、性能命令和 Spike 专用任务在建立后补入本节。安装启动冒烟将时间戳、APK 摘要、去标识设备能力与结果写入 `.local-evidence/phase-0/P0-APK-smoke-<timestamp>.json`；真实设备输出先进入 `.local-evidence/`，去敏摘要再进入 Git。

## 变更红线

以下操作必须先征得用户同意：删除文件或目录、修改密钥或 CI/CD、数据库 schema 变更或迁移、危险 Git 操作、安装全局依赖、修改系统配置、公开发布。
