# 去隐私合成测试数据

本目录只保存可公开、可重复生成的合成字段形状，不保存真实通知的打码副本。

每个数据集目录必须包含：

- `dataset.json`：稳定排序、UTF-8、LF 结尾的输入与期望定义；
- `metadata.properties`：数据集 ID、版本、固定种子、`dataset.json` 的 SHA-256、生成规则、期望摘要和变更原因。

运行 `.\tools\verify-testdata.ps1` 校验元数据、摘要与敏感哨兵。输入或期望发生语义变化时，新建下一版本目录；不得覆盖旧版本伪装成同一基线。
